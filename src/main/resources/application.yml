cico:
  key: CICO#
cloudinary:
  apiKey: 236829668753326
  apiSecretKey: XDI2Rc8Ty0kDn1rGOiAAWqTG6VI
  cloudName: df04kiqy3
secret: abhi9462
server:
  port: 8081
spring:
  cache:
    type: redis
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: ${MYSQL_PASSWORD:Java@2023}
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/fees_management?createDatabaseIfNotExist=true
    username: ${MYSQL_USERNAME:root}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      key-deserializer: 'org.apache.kafka.common.serialization.StringDeserializer '
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
    producer:
      bootstrap-servers: localhost:9092
      key-serializer: 'org.apache.kafka.common.serialization.StringSerializer '
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
  mail:
    host: smtp.google.com
    password: abhi@123
    port: 25
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    username: abhi
  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER
  redis:
    host: localhost
    port: 6379
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
staticPath: \src\main\resources\static\
